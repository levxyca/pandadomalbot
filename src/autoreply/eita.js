const patterns = [/^eita/i];

/**
 * Answer the "eita" message.
 *
 * @returns {string}
 */
function execute() {
  return `levxycEita levxycEita levxycEita`;
}

module.exports = {
  patterns,
  execute,
};
